using System;
using System.Text;

namespace Algorithms
{
    //http://codeforces.com/problemset/problem/584/C
    public class Task18
    {
        //public static string Func()
        //{
        //    var str1 = Console.ReadLine();
        //    var str2 = Console.ReadLine();
        //    var t = int.Parse(Console.ReadLine() ?? "0");
        //    var flag = true;
        //    var result = new StringBuilder("-1");

        //    if (string.IsNullOrEmpty(str1) || string.IsNullOrEmpty(str2) || str1.Length != str2.Length || t > str1.Length)
        //    {
        //        return result.ToString();
        //    }

        //    for (var i = 0; i < str1.Length; i++)
        //    {
        //        if (t > 0)
        //        {
        //            if (str1[i] != str2[i])
        //            {
        //                result.Append(flag ? str1[i] : str2[i]);
        //                flag = !flag;
        //            }
        //            else
        //            {
        //                var tmp = str1[i] + 1;
        //                result.Append(tmp);
        //            }
        //        }
        //        result.Append(tmp);
        //        t--;
        //    }

        //    return result.ToString();
        //}
    }
}